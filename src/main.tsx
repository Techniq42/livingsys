import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

// Sovereign OS v1.0 — Fellowship of Living Systems
createRoot(document.getElementById("root")!).render(<App />);
