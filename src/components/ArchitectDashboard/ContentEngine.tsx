export default function ContentEngine() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      Content Engine is offline — the `content_pieces` table is not in the current schema.
    </div>
  );
}
